<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.Fullscreen" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.ActionBar.Transparent" parent="AppTheme">
        <item name="android:actionBarStyle">@style/ActionBarTransparent</item>
        <item name="android:windowActionBarOverlay">true</item>
        <!-- Support library compatibility -->
        <item name="actionBarStyle">@style/ActionBarTransparent</item>
        <item name="windowActionBarOverlay">true</item>
    </style>


    <style name="ActionBarTransparent" parent="Widget.AppCompat.Light.ActionBar.Solid">
        <item name="android:background">@null</item>
        <!-- Support library compatibility -->
        <item name="background">@null</item>
    </style>

    <style name="Base.Widget.Design.TabLayout" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="tabBackground">@drawable/tab_bg</item>
        <item name="tabIndicatorColor">@color/colorGreyText</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="android:textSize">32sp</item>
        <item name="textAllCaps">true</item>
        <item name="tabSelectedTextColor">@color/colorGreyText</item>
    </style>
    <style name="TabText" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/colorGreyText</item>
    </style>

    <style name="tallerBarStyle" parent="@android:style/Widget.SeekBar">
        <item name="android:indeterminateOnly">false</item>
        <item name="android:minHeight">1dp</item>
        <item name="android:maxHeight">1dp</item>
    </style>
</resources>
